<UserControl x:Class="Gu.Wpf.FlipView.Demo.ControlDemos.TransitionControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:flipView="http://gu.se/FlipView"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controlDemos="clr-namespace:Gu.Wpf.FlipView.Demo.ControlDemos"
             d:DesignHeight="300"
             d:DesignWidth="300"
             mc:Ignorable="d">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type controlDemos:TransitionItem}">
            <Border HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="{Binding Brush}">
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="72"
                           Text="{Binding Text}" />
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <flipView:TransitionControl Content="{Binding SelectedItem, ElementName=ListBox}">
            <flipView:TransitionControl.InAnimation>
                <Storyboard>
                    <DoubleAnimation BeginTime="0:0:0"
                                     Duration="0:0:0.3"
                                     From="1"
                                     Storyboard.TargetProperty="Opacity"
                                     To="0" />
                    <DoubleAnimation BeginTime="0:0:0"
                                     Duration="0:0:0.3"
                                     From="0"
                                     Storyboard.TargetName="{x:Static flipView:TransitionControl.PART_OldContent}"
                                     Storyboard.TargetProperty="(flipView:Transform.RelativeOffsetX)"
                                     To="1" />
                </Storyboard>
            </flipView:TransitionControl.InAnimation>

            <flipView:TransitionControl.OutAnimation>
                <Storyboard>
                    <DoubleAnimation BeginTime="0:0:0"
                                     Duration="0:0:0.3"
                                     From="0"
                                     Storyboard.TargetProperty="Opacity"
                                     To="1" />
                    <DoubleAnimation BeginTime="0:0:0"
                                     Duration="0:0:0.3"
                                     From="-1"
                                     Storyboard.TargetName="{x:Static flipView:TransitionControl.PART_NewContent}"
                                     Storyboard.TargetProperty="(flipView:Transform.RelativeOffsetX)"
                                     To="0" />
                </Storyboard>
            </flipView:TransitionControl.OutAnimation>
        </flipView:TransitionControl>

        <ListBox x:Name="ListBox"
                 Grid.Row="1"
                 HorizontalContentAlignment="Stretch"
                 VerticalContentAlignment="Stretch"
                 ItemsSource="{Binding Items,
                                       RelativeSource={RelativeSource AncestorType={x:Type controlDemos:TransitionControlView}}}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding Items.Count, RelativeSource={RelativeSource AncestorType={x:Type controlDemos:TransitionControlView}}}" Rows="1" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
    </Grid>
</UserControl>
